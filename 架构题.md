## **模块一：Context / Performance**

### 2.一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？

对于连续 200 轮的 Session，我不会简单地删除前面的消息，或者只让模型生成一段摘要。因为上下文压缩的核心不是把文字变短，而是在有限 Token 预算下，保留后续回答真正需要的信息。具体方案需要根据 Agent 的使用场景来设计。

首先，如果是**普通问答或闲聊场景**，前后依赖较弱，我会采用“**滚动摘要加最近窗口**”的方式。当上下文达到一定 Token 阈值时，将较早的对话压缩成一份累计摘要，同时保留最近 10～20 轮完整消息。摘要保证模型知道之前聊过什么，近期原始消息则保留语气、指代关系和对话连贯性。这种方式实现简单、成本较低，但需要注意多次递归摘要可能产生信息失真，因此原始消息不能真正删除，而应继续保存在数据库中。

如果是 **Coding Agent、数据库 Agent** 等任务型场景，我认为只做自然语言摘要是不够的。除了记录“之前讨论了什么”，还需要**单独维护结构化任务状态**，例如当前目标、用户约束、已经完成的步骤、关键决策、失败操作、Todo 和待解决问题。比如用户早期明确提出“不能修改数据库表结构”，这类信息不能依赖摘要模型自行判断是否保留，而应该进入 constraints 字段，每轮都稳定注入 Context。自然语言摘要负责保证对话流畅，结构化状态负责保证任务执行正确。

如果一个 Session 中存在多个明显的话题或任务阶段，我会使用分层摘要，而不是维护一份不断变长的总摘要。例如将 200 轮对话划分为需求分析、架构设计、开发实现和测试修复几个阶段，每个阶段分别生成摘要，再生成一份全局摘要。用户当前在讨论测试问题时，只需要加载全局摘要、测试阶段摘要和最近对话，不必加载前面所有开发细节。这样可以减少无关信息干扰，也降低反复摘要带来的信息丢失。

对于很早但仍可能有用的信息，我会采用外部记忆召回。完整历史消息可以存入数据库，并建立向量、关键词、时间和实体索引。当用户再次提到某个历史问题时，再根据当前问题召回相关原始消息。例如用户问“我之前是不是说过不能修改表结构”，系统可以直接召回对应的原始对话，而不是完全相信摘要。召回时不能只看向量相似度，还需要结合时间、重要程度和当前任务状态，避免召回已经失效的旧信息。

如果是**医疗、法律、客服等要求可审计的场景**，我会更加保守。摘要只能作为模型输入优化手段，不能替代原始记录。关键结论应该保留对应的 message_id，出现争议时能够追溯原文。如果摘要与原始消息不一致，必须以原始消息为准。多模态场景也类似，图片或文件不应长期直接放在 Context 中，而是保存文件 ID、内容描述和结构化提取结果，需要时再重新加载原文件。

最终传给模型的 Context 可以由六部分组成：System Prompt、当前结构化任务状态、全局摘要、与本轮问题相关的历史记忆、最近若干轮完整消息，以及当前用户输入。压缩也不应该固定在第 200 轮触发，而应根据 Token 使用量、话题切换、任务阶段结束或大段工具输出等条件动态触发。

为了保证压缩后的对话仍然流畅，我会重点检查五类信息是否保留：当前目标、用户明确约束、已经作出的决定、未完成事项，以及最近对话中的指代关系。同时原始消息始终作为事实来源保留，摘要可以重新生成。测试时也不能只看摘要是否通顺，而应该验证模型能否记住早期关键约束、识别用户后来修改的决定、继续未完成任务，以及避免重复已经失败的操作。

因此，我更倾向于使用“**最近窗口＋分层摘要＋结构化状态＋按需召回**”的混合方案。它相比单纯截断或单一摘要实现更复杂，但能够在控制 Token 成本的同时，兼顾对话流畅性、任务准确性和历史信息可追溯性。



## **模块二：Memory**

###  1.和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？

当用户和聊天 Agent 熟悉半个月后，再次问了一个以前问过的问题，我认为不能简单做一次向量检索，然后把旧答案原样返回。因为用户重复提问，可能是忘记了答案，也可能是上次没有理解、现实情况发生了变化，或者只是想重新确认。因此，Memory 召回的重点不是“能不能搜到”，而是“这条记忆现在是否还应该被使用”。

首先，系统应判断本轮是否需要召回长期记忆。对于普通知识问题，可以召回上次的提问、回答以及用户当时没有理解的部分，从新的角度解释，而不是直接提醒用户“你以前问过”。如果是职业选择、个人偏好、项目状态等可能变化的问题，则需要检查记忆的时间和有效性，优先使用用户最近一次明确表达的信息，不能让半个月前的旧状态覆盖当前情况。

在召回方式上，我不会只使用向量相似度，而会结合语义相关性、关键词、时间、重要程度和当前任务进行多路检索。召回候选结果后，还要做去重、重排和冲突检测。例如系统同时找到“用户使用 MySQL”和“用户后来改用 PostgreSQL”，应该判断新信息是否已经使旧信息失效，而不是把两条矛盾记忆全部交给模型猜测。

记忆本身也需要分类管理。我会区分用户长期偏好、事实信息、历史对话经历和任务状态。对于“用户不喜欢过于复杂的回答”这种稳定偏好，可以长期保留；对于“用户明天参加面试”这种短期信息，则应设置有效期；对于重要结论还应保存对应的原始 message_id，避免只依赖模型生成的摘要。

目前一些开源方案也体现了这些思路。例如 Mem0 会从对话中提取重要事实，并对记忆进行新增、更新或删除；Letta/MemGPT 采用分层记忆，将重要信息放在当前 Context，其他信息按需召回；Graphiti 使用时间知识图谱处理事实随时间变化的问题。Generative Agents 则提出根据相关性、时效性和重要度综合选择记忆。

因此，我会把完整流程设计为：先判断是否需要长期记忆，再进行多路召回，之后完成有效性判断、冲突处理和重排，只把少量高质量记忆注入当前 Context。本轮对话结束后，再根据用户的新表达决定新增、更新或废弃旧记忆。

我认为合理的 Memory 系统不是让 Agent 什么都记住，而是让它在正确的时候找到仍然有效的信息，并且知道什么时候不能相信自己的旧记忆。



## **模块三：Task** 

### 1.对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？

长程任务中，大模型“忘记目标”通常不只是上下文太长，还包括执行过程逐渐偏离目标，以及任务中断后无法恢复。因此，不能只靠重复 Prompt，而应将目标和进度交给 Runtime 管理。

| 方案             | 核心思路                                         | 优点                                | 缺点                              |
| ---------------- | ------------------------------------------------ | ----------------------------------- | --------------------------------- |
| 目标重注入       | 每轮重新提供最终目标、约束和成功条件             | 简单、成本低                        | 只能提醒目标，无法记录真实进度    |
| 结构化任务状态   | 单独维护目标、Todo、已完成步骤、失败记录和阻塞项 | 状态清晰，可观察、可恢复            | 需要设计 Schema，状态更新可能出错 |
| 分层规划         | 将长任务拆成多个子目标，逐阶段执行               | 降低单次推理难度，便于验证          | 初始计划可能不准确，容易机械执行  |
| 进度检查与重规划 | 定期判断是否有进展，走偏时重新规划               | 能从错误路径中恢复                  | 反思本身也可能出错，增加调用成本  |
| Checkpoint       | 持久化任务状态、工具结果和当前步骤               | 支持中断恢复、重试和审计            | 只能恢复状态，不能纠正错误方向    |
| 状态机或工作流   | 将固定流程交给程序控制，模型只负责局部决策       | 稳定、可控，适合高风险任务          | 灵活性较低，开发成本较高          |
| 多 Agent 分工    | 主 Agent 管理目标，子 Agent 执行具体任务         | 可拆分复杂任务，减少单个 Agent 负担 | 通信成本高，容易出现状态不一致    |

不同场景应该采用不同方案。固定业务流程，如审批、定时报表，更适合使用状态机、结构化状态和 Checkpoint，而不是让模型自由规划。Coding Agent、研究 Agent 等开放任务，则适合使用任务账本、Todo、分层规划、阶段验证和动态重规划。涉及删除数据、付款或发布生产环境等高风险操作时，还必须加入权限控制和人工确认。

我的观点是，不能依赖大模型在长上下文中“自己记住目标”。更合理的设计是把大模型当作一个根据当前状态选择下一步动作的执行器，由 Runtime 外置维护：

```text
最终目标
+ 用户约束
+ 当前子目标
+ 已完成步骤
+ 失败记录
+ 阻塞问题
+ 成功条件
```

每完成一个步骤就更新状态并保存 Checkpoint；连续多次失败、长期没有进展或环境发生变化时，再触发重新规划。这样可以分别解决目标遗忘、执行走偏和任务中断三个问题，比单纯增加上下文或重复 System Prompt 更可靠。



## **模块四：Tool / Session Runtime**

### 2.如果 session state 为 busy，此时用户又发来新消息，或者异步工具完成事件也到达，runtime 应该如何处理？

我认为，`busy` 不应该表示 Session 拒绝接收新事件，而应该表示：**同一时刻只能有一个执行流修改 Session State。**

因此，我会采用“事件队列 + Session 单写”的设计。用户消息、异步工具结果、取消、超时等都先转换为事件，进入该 Session 的 Mailbox，再由 Runtime 串行消费并更新状态，避免多个线程同时修改 messages、任务进度和工具状态。

对于 busy 时到达的用户消息，不能统一排队，而应根据意图处理：

| 用户消息类型     | 处理策略                               |
| ---------------- | -------------------------------------- |
| 补充当前任务信息 | 在当前原子步骤结束后中断，并合并新信息 |
| 修改目标或约束   | 暂停当前 Run，更新目标后重新规划       |
| 明确取消任务     | 触发取消事件，停止后续步骤             |
| 完全无关的新任务 | 排队，或新建独立 Session               |
| 高风险操作进行中 | 优先排队，必要时拒绝插入               |

用户消息不能直接插入正在执行的 LLM 或 Tool Call 中间，否则容易破坏调用顺序和上下文一致性。更合理的方式是在安全点保存 Checkpoint 后，再决定继续、取消或重规划。

对于异步工具完成事件，必须通过 `run_id` 和 `tool_call_id` 找到原来的调用：

- 原 Run 仍在等待：写入工具结果并继续执行；
- 原 Run 已重新规划：判断结果是否仍然有效，无效则标记为 `stale`；
- 原 Run 已取消：记录为迟到事件，不再注入当前 Context；
- 工具已经产生外部副作用：即使任务取消，也必须保留记录，必要时执行补偿操作。

同时还需要保证：

- 使用 `event_id` 防止事件重复执行；
- 使用版本号或序号保证事件顺序；
- 工具尽量设计为幂等，避免重试造成重复副作用；
- 多个异步工具并发完成时，可以并发收集结果，但 Session State 仍然串行更新。

我的核心观点是：

> busy 不是“不能接收消息”，而是“不能并发修改状态”。用户消息负责改变任务意图，工具事件负责完成已有执行，两者必须进入统一事件流，由 Runtime 根据优先级、所属 Run 和当前状态进行仲裁。

这样既能保证用户交互及时，也能避免异步事件把 Session State 写乱。



## **模块五：Agent Runtime 架构对比**

### 1.Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？

## Claude Code 与 OpenAI-compatible Function Calling 的区别

我认为这道题首先要区分两个层次：**Claude Code 是一套完整的 Agent Runtime，而 GLM、豆包等通常提供的是 OpenAI-compatible 工具调用协议。**因此，不能只比较字段格式，还要比较它们对工具执行过程的整体设计。

| 对比点       | Claude / Claude Code                | GLM、豆包等 OpenAI-compatible  |
| ------------ | ----------------------------------- | ------------------------------ |
| 工具调用格式 | `content` 中的 `tool_use` block     | `tool_calls` 字段              |
| 工具结果格式 | `user` 消息中的 `tool_result` block | 独立的 `role=tool` 消息        |
| 参数类型     | `input` 通常直接是 JSON 对象        | `arguments` 通常是 JSON 字符串 |
| 错误表达     | 原生支持 `is_error`                 | 一般由业务自行约定             |
| 多模态结果   | 可返回文本、图片、文档等内容块      | 通常以文本或 JSON 为主         |
| 生态兼容性   | 更贴合 Claude Agent                 | SDK、框架和国内模型兼容性更好  |

Claude 的设计更强调“内容块”。文本、工具调用和工具结果都可以放在统一且有顺序的消息结构中，表达能力更强，也更适合复杂 Agent 和多模态工具。但它对消息顺序要求严格，接入其他模型时通常需要做协议转换。

OpenAI-compatible 的优势是标准化和生态成熟。国内模型只要兼容 `tools`、`tool_calls` 和 `role=tool`，就能较低成本接入现有 SDK、LangChain 或自研 Runtime。缺点是不同厂商虽然接口形式相似，但在并行调用、流式参数、JSON Schema 和错误处理上仍可能存在差异，所以生产环境不能只修改 `base_url`，仍需要 Provider Adapter。

我认为两者真正的差距不在 JSON 字段，而在 **Harness 层**。Claude Code 除了传递工具调用，还实现了权限审批、工具超时、后台任务、长输出截断与落盘、Session 恢复和 Context 压缩。普通 OpenAI-compatible 接口只告诉开发者“模型想调用什么工具”，其余能力都需要 Runtime 自己实现。

因此，我的结论是：

> Claude 的协议表达能力更强，适合构建复杂 Agent；OpenAI-compatible 的优势是标准化和迁移成本低。但 Agent 是否可靠，最终取决于上层 Runtime，而不是工具调用格式本身。GLM 或豆包使用兼容协议，同样可以通过完善的 Runtime 实现接近 Claude Code 的能力。